let PostPost;_0e3‍.x([["getPosts",()=>getPosts],["createPost",()=>createPost]]);_0e3‍.w("../models/postPost.js",[["default",["PostPost"],function(v){PostPost=v}]]);

       const getPosts = async (req, res) => { 
    try{
        const postPosts = await PostPost.find();
        

        res.status(200).json(postPosts);
    } catch (error){
        res.status(404).json({message: error.message});
    }
}

       const createPost = async (req, res) => {
    const post = req.body;
    const newPost = new PostPost(post);
    try{
        await newPost.save();
        res.status(201).json(newPost);

    } catch (error){
        res.status(409).json({message: error.message});
    }
}